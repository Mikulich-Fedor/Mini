// Start
import { btnStart, start } from "./utils/start";

btnStart.addEventListener("click", start);
//
import { checkAll, btnSubmit } from "./utils/enter";

btnSubmit.addEventListener("click", checkAll);

import { payBitcoin, sorry } from "./utils/office";
payBitcoin.addEventListener("click", sorry);
